function alertarTexto(texto, parametro)
{
    // 1 exitoso
    // 2 fallido
    if(parametro === 1)
    {
        alertify.log(texto);
    }
    else if (parametro === 2)
    {
        alertify.error(texto);
    }
}